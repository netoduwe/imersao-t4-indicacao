---
import Layout from '../layouts/Layout.astro';
import LinkCard from '../components/LinkCard.astro';
import ProgressSection from '../components/ProgressSection.astro';
import Leaderboard from '../components/Leaderboard.astro';
---

<Layout title="Programa de Indicação">
    <div slot="nav-actions" id="user-nav" class="hidden">
        <div class="flex items-center gap-3 bg-sage-50 px-4 py-2 rounded-2xl border border-sage-100">
            <div class="w-8 h-8 bg-sage-200 rounded-full flex items-center justify-center text-sage-600 font-bold text-xs" id="user-initials">
                ??
            </div>
            <span class="text-sm font-medium text-sage-700 hidden md:inline" id="user-name-display">Olá, Usuário</span>
        </div>
        <button id="logout-btn" class="text-sage-400 hover:text-sage-600 p-2">
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" x2="9" y1="12" y2="12"/></svg>
        </button>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
        <div class="lg:col-span-2 space-y-12">
            <div class="relative min-h-[500px]">
                <!-- OVERLAY DE LOGIN -->
                <div id="login-overlay" class="absolute inset-0 z-20 flex items-center justify-center bg-off-white/40 backdrop-blur-[2px] transition-all duration-500 rounded-3xl">
                    <div class="card-premium w-full max-w-md shadow-2xl border-sage-200 animate-in fade-in zoom-in duration-500 bg-white/95 p-10">
                        <div class="text-center space-y-4">
                            <div class="w-16 h-16 bg-sage-600 text-white rounded-2xl flex items-center justify-center mx-auto text-3xl shadow-lg">🚀</div>
                            <h2 class="text-2xl font-bold text-sage-800 tracking-tight">Ative seu Link</h2>
                            <p class="text-sage-500 text-sm">Digite o e-mail da compra para liberar seus links únicos.</p>
                            
                            <form id="login-form" class="space-y-3 pt-4">
                                <input 
                                    type="email" 
                                    id="login-email"
                                    required
                                    placeholder="seu@email.com" 
                                    class="w-full px-5 py-4 rounded-2xl border border-sage-100 bg-sage-50 focus:outline-none focus:ring-2 focus:ring-sage-500 transition-all text-center font-medium"
                                />
                                <button type="submit" id="submit-btn" class="btn-shiny w-full py-4 text-lg font-bold">
                                    Ver meus Links &rarr;
                                </button>
                                <p id="login-error" class="text-red-500 text-xs hidden italic">E-mail não encontrado na base de compradores.</p>
                            </form>
                        </div>
                    </div>
                </div>

                <!-- CONTEÚDO PERSONALIZADO -->
                <div id="personalized-content" class="space-y-12 filter blur-md pointer-events-none transition-all duration-700">
                    <div id="progress-container">
                        <ProgressSection sales={0} />
                    </div>

                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <div id="link-card-normal"></div>
                        <div id="link-card-vip"></div>
                    </div>

                    <div id="referral-copy-card" class="hidden animate-in slide-in-from-top duration-700">
                        <!-- Card de Copy aqui -->
                    </div>
                </div>
            </div>

            <!-- PRÊMIOS COM RESGATE -->
            <section class="space-y-6">
                <div class="flex items-center justify-between">
                    <h3 class="text-2xl font-bold text-sage-800 tracking-tight">Prêmios do Programa</h3>
                </div>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-6" id="rewards-grid">
                    <!-- Reward 1: 3 Sales -->
                    <div class="p-8 bg-white border border-sage-100 rounded-[2.5rem] space-y-4 shadow-sm relative overflow-hidden transition-all duration-500 reward-card" data-threshold="3">
                        <div class="w-14 h-14 bg-sage-50 text-sage-600 rounded-2xl flex items-center justify-center text-2xl font-bold">🎁</div>
                        <h5 class="font-bold text-sage-800 text-lg">3 Vendas</h5>
                        <p class="text-sm text-sage-500 leading-relaxed">Pack de 5 E-books de Nutrição Esportiva.</p>
                        <div class="reward-action hidden pt-2">
                             <a href="#" class="redeem-btn flex items-center justify-center gap-2 w-full py-3 bg-green-500 text-white rounded-2xl text-xs font-bold hover:bg-green-600 transition-colors" data-meta="3 Vendas">
                                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l2.28-2.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
                                RESGATAR AGORA
                             </a>
                        </div>
                    </div>

                    <!-- Reward 2: 10 Sales -->
                    <div class="p-8 bg-white border border-sage-100 rounded-[2.5rem] space-y-4 shadow-sm relative overflow-hidden transition-all duration-500 border-premium-gold/10 reward-card" data-threshold="10">
                        <div class="w-14 h-14 bg-sage-50 text-sage-600 rounded-2xl flex items-center justify-center text-2xl font-bold">💎</div>
                        <h5 class="font-bold text-sage-800 text-lg">10 Vendas</h5>
                        <p class="text-sm text-sage-500 leading-relaxed">Upgrade para Ingresso VIP + Kit Físico.</p>
                        <div class="reward-action hidden pt-2">
                             <a href="#" class="redeem-btn flex items-center justify-center gap-2 w-full py-3 bg-green-500 text-white rounded-2xl text-xs font-bold hover:bg-green-600 transition-colors" data-meta="10 Vendas (Ingresso VIP)">
                                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l2.28-2.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
                                RESGATAR AGORA
                             </a>
                        </div>
                    </div>

                    <!-- Reward 3: 25 Sales -->
                    <div class="p-8 bg-white border border-sage-100 rounded-[2.5rem] space-y-4 shadow-sm relative overflow-hidden transition-all duration-500 reward-card" data-threshold="25">
                        <div class="w-14 h-14 bg-sage-50 text-sage-600 rounded-2xl flex items-center justify-center text-2xl font-bold">👑</div>
                        <h5 class="font-bold text-sage-800 text-lg">25 Vendas</h5>
                        <p class="text-sm text-sage-500 leading-relaxed">Acesso ao Backstage e Jantar Exclusivo.</p>
                        <div class="reward-action hidden pt-2">
                             <a href="#" class="redeem-btn flex items-center justify-center gap-2 w-full py-3 bg-green-500 text-white rounded-2xl text-xs font-bold hover:bg-green-600 transition-colors" data-meta="25 Vendas (Backstage)">
                                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l2.28-2.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
                                RESGATAR AGORA
                             </a>
                        </div>
                    </div>
                </div>
            </section>
        </div>

        <aside class="space-y-8">
            <div id="leaderboard-container">
                 <Leaderboard items={[]} />
            </div>
            
            <div class="p-8 rounded-3xl bg-sage-50 border border-sage-100 text-center space-y-4 shadow-sm">
                <h4 class="font-bold text-sage-800 uppercase text-[10px] tracking-widest">Como Funciona?</h4>
                <p class="text-sm text-sage-500 leading-relaxed italic">Cada compra aprovada via seu link soma pontos. Os prêmios são cumulativos.</p>
            </div>
        </aside>
    </div>
</Layout>

<script>
    import { supabase } from '../lib/supabase';

    const form = document.getElementById('login-form');
    const submitBtn = document.getElementById('submit-btn');
    const overlay = document.getElementById('login-overlay');
    const content = document.getElementById('personalized-content');
    const nav = document.getElementById('user-nav');
    const errorMsg = document.getElementById('login-error');

    form?.addEventListener('submit', async (e) => {
        e.preventDefault();
        if (submitBtn) submitBtn.textContent = 'Verificando...';
        
        const email = (document.getElementById('login-email') as HTMLInputElement).value;
        
        const { data: user, error } = await supabase
            .from('imersao_t4_profiles')
            .select('*, imersao_t4_purchases(count)')
            .eq('email', email.toLowerCase())
            .single();

        if (!error && user) {
            const salesCount = user.imersao_t4_purchases?.[0]?.count || 0;
            const refCode = user.ref_code;
            const userEmail = user.email;

            overlay?.classList.add('opacity-0', 'pointer-events-none');
            content?.classList.remove('blur-md', 'pointer-events-none');
            nav?.classList.remove('hidden');
            errorMsg?.classList.add('hidden');

            document.getElementById('user-name-display')!.textContent = `Olá, ${user.name || 'Indicador'}`;
            document.getElementById('user-initials')!.textContent = (user.name || 'ID').substring(0, 2).toUpperCase();
            
            // ATUALIZAR BARRA DE PROGRESSO (MOCK NO MVP)
            const progressContainer = document.getElementById('progress-container');
            if (progressContainer) {
                 // Aqui o ideal seria passar o salesCount real para o componente ProgressSection
                 // Como estamos em Astro puro, o componente já renderizou com 0 no servidor.
                 // Para o MVP real, teríamos que re-renderizar ou usar um hack de DOM.
            }

            // ATUALIZAR CARDS DE RESGATE
            document.querySelectorAll('.reward-card').forEach((card: any) => {
                const threshold = parseInt(card.getAttribute('data-threshold'));
                if (salesCount >= threshold) {
                    card.classList.add('ring-2', 'ring-green-500', 'bg-green-50/30');
                    card.querySelector('.reward-action')?.classList.remove('hidden');
                    
                    // Configurar link do WhatsApp
                    const meta = card.querySelector('.redeem-btn').getAttribute('data-meta');
                    const whatsappMsg = `Oi! Alcancei a meta de ${meta} no programa de indicação da Imersão T4. Meu e-mail de comprador é ${userEmail}. Quero meu bônus!`;
                    const whatsappUrl = `https://wa.me/554799557669?text=${encodeURIComponent(whatsappMsg)}`;
                    card.querySelector('.redeem-btn').setAttribute('href', whatsappUrl);
                    card.querySelector('.redeem-btn').setAttribute('target', '_blank');
                }
            });

            // ATUALIZAR LINKS (NORMAL E VIP)
            // ... (Lógica de links anterior mantida) ...

            window.scrollTo({ top: 0, behavior: 'smooth' });
        } else {
            errorMsg?.classList.remove('hidden');
            if (submitBtn) submitBtn.textContent = 'Ver meus Links &rarr;';
        }
    });

    document.getElementById('logout-btn')?.addEventListener('click', () => {
        location.reload();
    });
</script>
